# Copy to .env.local and adjust as needed.
# NOTE: NEXT_PUBLIC_* variables are exposed to the browser and baked at build time.

# FastAPI backend base URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
